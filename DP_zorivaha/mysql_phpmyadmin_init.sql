-- Run this in phpMyAdmin (SQL tab) as root without password.
-- Creates project database with proper UTF-8 settings.

CREATE DATABASE IF NOT EXISTS `zori_vaha`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE `zori_vaha`;
